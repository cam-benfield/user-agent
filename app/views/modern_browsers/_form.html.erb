<%= form_for @modernBrowser do |f| %>

  <% if @modernBrowser.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@modernBrowser.errors.count, "error") %> prohibited
        this modernBrowser from being saved:
      </h2>
      <ul>
        <% @modernBrowser.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :company %><br>
    <%= f.text_field :company %>
  </p>

  <p>
    <%= f.label :information %><br>
    <%= f.text_area :information %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
